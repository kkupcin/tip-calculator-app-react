import { StyledResetButton } from "../styled/StyledResetButton.styled";

const ResetButton = () => {
  return <StyledResetButton className="disabled">Reset</StyledResetButton>;
};

export default ResetButton;
